import classes from './RightSideBar.module.scss';

const RightSidebarBar = () => {
	return (
		<aside className={classes.sidebar__progressbar}>
			progress
		</aside>
	);
};

export default RightSidebarBar;